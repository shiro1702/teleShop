# /dashboard/team/roles

- Экран настройки уровней доступа в организации.
- Цель: зафиксировать кто и что может делать в дашборде.

## Что должно быть тут

- Таблица ролей:
  - `Owner` — полный доступ;
  - `Manager` — ограниченный доступ по permissions.
- Матрица permissions по разделам:
  - заказы (просмотр/смена статуса),
  - филиалы (просмотр/создание/редактирование),
  - меню (просмотр/редактирование),
  - настройки организации (просмотр/редактирование webhook и токенов),
  - команда (приглашать/удалять менеджеров).
- Отдельный контур прав для сотрудников, которые переводят заказы по статусам:
  - кто может переносить карточки в канбане,
  - какие статусы доступны для перехода,
  - в каких филиалах и в какие смены это разрешено.
- Переключатели прав для роли `Manager`.
- История изменений прав (кто изменил, когда).

## Базовые правила (MVP)

- Только `Owner` может:
  - назначать/снимать `Owner`,
  - выдавать критичные permissions,
  - удалять менеджеров из организации.
- `Manager` по умолчанию:
  - видит заказы и филиалы,
  - не может управлять составом команды и критичными настройками.

## Техническая логика (ожидаемая)

- Источник прав: `profiles.role` + `permissions` (jsonb) или отдельная membership-таблица.
- Проверки на сервере обязательны (нельзя полагаться только на UI).
- UI скрывает/блокирует действия без разрешения, но итоговая проверка всегда в API.

## Что уже можно делать

- Определять роли `Owner` и `Manager`.
- Управлять матрицей permissions для ключевых разделов.
- Применять базовые ограничения: критичные действия только для `Owner`.

## Недочеты / риски

- Не описан полный жизненный цикл изменения роли (кто и когда подтверждает).
- Нет формального набора "критичных permissions" и их версии.
- Не зафиксирован процесс восстановления доступа при ошибочной настройке прав.
- Не выделена отдельная роль/набор прав для операторов, которые двигают статусы заказов.

## План работ

### Сейчас (MVP)

- [ ] Зафиксировать перечень критичных permissions.
- [ ] Добавить обязательное подтверждение при изменении чувствительных прав.
- [ ] Добавить журнал изменений ролей и прав.
- [ ] Ввести permissions для "оператора заказов" (просмотр заказов + ограниченные переходы статусов).

### Потом

- [ ] Добавить готовые role templates для типовых команд.
- [ ] Добавить сравнение прав "до/после" перед сохранением.
- [ ] Добавить периодическую проверку избыточных прав.
- [ ] Добавить гибкие правила переходов статусов по филиалу/смене/типу сотрудника.

### Отложить

- [ ] Добавить RBAC + ABAC гибридную модель для сложных кейсов.
- [ ] Реализовать approval-flow для высокорисковых изменений прав.
