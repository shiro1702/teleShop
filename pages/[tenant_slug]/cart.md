# /:tenant_slug/cart

- Legacy tenant-корзина без города.
- Для мульти-города мигрировать на `/:city_slug/:tenant_slug/cart`.

Требования к строке корзины (параметры, модификаторы): `pages/[city_slug]/[tenant_slug]/cart.md`.
