# /cart

- Legacy маршрут корзины без города.
- Должен быть совместимым редиректом на `/:city_slug/cart`.

Детали UX корзины (параметры позиции, модификаторы) — в `pages/[city_slug]/[tenant_slug]/cart.md`.
