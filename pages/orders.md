# /orders

- Страница клиента со всеми заказами по разным ресторанам.
- Отдельный блок активных заказов (`new`, `in_progress`).
- Фильтры: поиск по ресторану, активные/история, сортировка.
- Источник данных: `GET /api/client-orders` по `customer_profile_id`.
