Развернуть свой Supabase (Self-hosted) на российском сервере — это самое грамотное решение для твоего проекта.

Во-первых, ты остаешься на привычных технологиях (код переписывать не надо). Во-вторых, ты автоматически выполняешь ФЗ-152 о персональных данных (адреса и телефоны клиентов хранятся в РФ), что станет крутым аргументом при продажах рестораторам («С нами вам не прилетит штраф от Роскомнадзора»).

Процесс делится на 3 этапа. Глаза боятся — Docker делает. Погнали!

Этап 1: Аренда сервера в РФ
Supabase состоит из кучи микросервисов (Postgres, авторизация, хранилище, реалтайм и т.д.), поэтому он довольно прожорливый. Дешевый сервер за 200 рублей его не потянет.

Где брать: Timeweb Cloud, Selectel, Яндекс.Облако, Beget.
Характеристики (Минимум): 4 ядра CPU, 8 ГБ оперативной памяти (RAM), SSD-диск от 40 ГБ. OS: Ubuntu 22.04 или 24.04.
Цена вопроса: Примерно 800–1200 руб/мес (это дешевле облачного Супабейса при масштабировании).
Этап 2: Установка Supabase на сервер
Подключаешься к серверу по SSH (через консоль или Termius) и выполняешь команды по шагам:

1. Ставишь Docker и Git (если их нет на сервере):

bash Скопировать Открыть блок
2. Скачиваешь официальный репозиторий Supabase:

bash Скопировать Открыть блок
3. Настраиваешь доступы (самый важный шаг): Копируешь пример файла настроек:

bash Скопировать Открыть блок
Теперь нужно открыть этот файл .env (например, командой nano .env) и обязательно изменить следующие параметры, чтобы тебя не взломали:

POSTGRES_PASSWORD= (придумай сложный пароль)
JWT_SECRET= (сгенерируй любую случайную строку символов на 40, это для токенов)
SITE_URL= (поставь IP своего сервера или домен, если уже привязал).
ANON_KEY= и SERVICE_ROLE_KEY= (надо сгенерировать новые. В документации Supabase есть ссылка на генератор JWT-токенов).
4. Запускаешь систему:

bash Скопировать Открыть блок
Всё! Минут через 5-10 все контейнеры поднимутся.

Твоя админка (Supabase Studio) будет доступна в браузере по адресу: http://IP_ТВОЕГО_СЕРВЕРА:8000
Твой API будет на: http://IP_ТВОЕГО_СЕРВЕРА:8000
Этап 3: Перенос проекта (Миграция)
Облачный Supabase — это просто красивая обертка над базой данных PostgreSQL. Поэтому перенос — это банальный дамп (копия) базы.

Шаг 1: Перенос Базы Данных и Пользователей Так как пользователи в Supabase хранятся в той же базе (в схеме auth), мы перенесем всё за один раз. На своем компьютере (где установлен PostgreSQL) вводишь команду для скачивания данных из облака:

bash Скопировать Открыть блок
(Пароль базы от облачного проекта можно посмотреть в настройках Database в облачном Supabase).

Затем загружаешь этот файл на свой новый сервер и разворачиваешь дамп:

bash Скопировать Открыть блок
Шаг 2: Перенос файлов (Картинок блюд и т.д.) К сожалению, Storage в Supabase переносится сложнее. Самый простой путь:

Скачать все картинки из облачного Supabase Storage на комп.
Открыть твою новую админку (http://IP_СЕРВЕРА:8000), зайти в раздел Storage.
Создать те же бакеты (папки) и просто руками перетащить туда скачанные картинки. Если их сотни — можно написать простенький скрипт на NodeJS.
Шаг 3: Переключение твоих приложений Идешь в код своего Telegram-бота, фронтенда сайта и Mini App. Находишь там переменные окружения, где ты подключал Supabase. Меняешь старые данные на новые:

env Скопировать Открыть блок
⚠️ Подводные камни (О чем надо знать заранее):
HTTP против HTTPS. Telegram Mini Apps и браузеры требуют защищенного соединения (HTTPS). Просто по IP-адресу с http:// бот работать откажется. Тебе придется купить домен (например, api-food.ru), привязать его к IP сервера и поставить Nginx + бесплатный SSL-сертификат от Let's Encrypt. Это стандартная процедура, делается за 15 минут.
Email авторизация. В облаке почтовые уведомления о регистрации ходили через сервера Supabase. На своем сервере тебе придется подключить сторонний SMTP-сервер (например, бесплатный Яндекс.Почта для домена или Mail.ru), вписав его данные в тот самый файл .env.
Если процесс кажется сложным с точки зрения администрирования серверов, можно нанять DevOps-инженера на Kwork за 2-3 тысячи рублей — он поднимет тебе этот Docker-контейнер и прикрутит SSL-сертификат за пару часов.

Переход стоит того! Ты получишь полный контроль над данными, отсутствие лимитов облака и независимость от санкций.

 Скопировать
 Gemini 3.1 Pro
22.04.2026 08:05:38 GMT+3
Нас надо еще на том же сервере разместить наш проект на nuxt + nitro

 Скопировать
Logo
Отличный план! Разместить всё на одном сервере — это логично и экономно.

Поскольку у тебя на одном сервере будут работать сразу два приложения (Supabase и твой Nuxt), они не могут одновременно занимать стандартный порт для сайтов (80/443).

Нам нужен «регулировщик», который будет смотреть: "Ага, запрос пришел на твой-сайт.ру — отправляю его в Nuxt. А этот запрос пришел на api.твой-сайт.ру — отправляю его в Supabase". Этим регулировщиком будет веб-сервер Nginx.

Плюс Telegram Mini Apps требуют обязательного наличия HTTPS (SSL-сертификата).

Вот пошаговая инструкция, как поселить Nuxt рядом с Supabase и настроить домены.

Подготовка: Домены
Перед началом зайди к регистратору своего домена (Reg.ru, Beget и т.д.) и создай две А-записи, которые смотрят на IP твоего сервера:

твой-домен.ru (для Nuxt)
api.твой-домен.ru (для Supabase)
Шаг 1: Установка Node.js и PM2 (Для Nuxt)
Nuxt 3 с Nitro бэкендом под капотом собирается в Node.js приложение. Чтобы оно работало в фоне и автоматически перезапускалось при сбоях, мы используем менеджер процессов PM2.

Заходи на сервер и выполняй:

bash Скопировать Открыть блок
Шаг 2: Загрузка и запуск Nuxt-проекта
На своем рабочем компе в папке с проектом выполни сборку:

bash Скопировать Открыть блок
У тебя появится папка .output. Перенеси её на свой сервер (например, через файловый менеджер в Termius, FileZilla или SCP) в папку, скажем, /var/www/nuxt-app/.

Перейди в эту папку на сервере:

bash Скопировать Открыть блок
Запусти проект через PM2 (укажем, чтобы он работал на порту 3000):

bash Скопировать Открыть блок
Сохрани настройки PM2, чтобы Nuxt запускался сам при перезагрузке сервера:

bash Скопировать Открыть блок
Всё, твой фронт+бэк на Nitro работает на порту 3000!

Шаг 3: Настройка Nginx (Регулировщик)
Теперь свяжем домены с нашими приложениями.

Устанавливаем Nginx:

bash Скопировать Открыть блок
Создаем файл настроек. Открываем редактор:

bash Скопировать Открыть блок
Вставляем туда этот конфиг (замени твой-домен.ru на свой реальный):

nginx Скопировать Открыть блок
Блок для Supabase (База и API)
server { listen 80; server_name api.твой-домен.ru;

location / {
    proxy_pass http://127.0.0.1:8000; # Порт, где висит Supabase
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection 'upgrade';
    proxy_set_header Host $host;
    proxy_cache_bypass $http_upgrade;
}
}

*Нажми `Ctrl+O`, `Enter` (сохранить), `Ctrl+X` (выйти).*

4. Включаем этот конфиг и перезапускаем Nginx:
```bash
sudo ln -s /etc/nginx/sites-available/myapp /etc/nginx/sites-enabled/
sudo nginx -t # Проверка, что нет ошибок
sudo systemctl restart nginx
Шаг 4: Прикручиваем HTTPS (SSL-сертификаты)
Без этого шага Telegram Mini Apps не откроются. Сделаем сертификаты бесплатными от Let's Encrypt.

Устанавливаем Certbot:

bash Скопировать Открыть блок
Запускаем магию автоматической настройки (введи эту команду):

bash Скопировать Открыть блок
Certbot спросит твой email (туда придут уведомления, если сертификат забудет обновиться), попросит согласиться с правилами (жми Y) и предложит выбрать твои домены из списка (нажми Enter, чтобы выбрать оба).

Готово! Certbot сам перепишет конфиг Nginx, добавит туда сертификаты и сделает автоматическую переадресацию с HTTP на HTTPS.

Как это теперь работает:
Клиент открывает Mini App по ссылке https://твой-домен.ru. Запрос идет в Nginx -> Nginx перекидывает его в Nuxt (порт 3000).
Твой Nuxt делает запрос за пользователями/заказами по адресу https://api.твой-домен.ru. Запрос идет в Nginx -> Nginx перекидывает его в Supabase (порт 8000).
P.S. Важный момент про ресурсы сервера: Supabase (в докере) + Nuxt + база данных съедят минимум 2-3 ГБ оперативной памяти просто в покое. Убедись, что твой сервер имеет хотя бы 4, а лучше 8 ГБ RAM, чтобы при наплыве гостей на бизнес-ланч ничего не упало.

 Скопировать
 Gemini 3.1 Pro
22.04.2026 08:11:45 GMT+3
Не забудь про git и автодеплой

 Скопировать
Logo
Абсолютно в точку! Перекидывать папки через FTP или FileZilla руками в 2024 году — это преступление против разработки.

Поскольку у нас на сервере уже крутится прожорливый Supabase, мы не будем нагружать сервер сборкой проекта (команда npm run build может сожрать всю оперативку и «уронить» базу данных в самый неподходящий момент).

Мы настроим умный автодеплой через GitHub Actions:

Ты пушишь код в ветку main.
GitHub на своих бесплатных серверах скачивает пакеты и собирает проект.
GitHub по SSH закидывает готовую папку .output на твой сервер.
GitHub дает команду PM2 перезапустить приложение.
Всё это происходит за пару минут в фоновом режиме. Вот как это настроить:

Шаг 1: Создаем SSH-ключ для GitHub
Гитхабу нужен защищенный доступ к твоему серверу.

Зайди на свой сервер по SSH и сгенерируй ключ (без пароля, просто жми Enter на все вопросы):
bash Скопировать Открыть блок
Добавь этот ключ в список разрешенных на сервере:
bash Скопировать Открыть блок
Теперь выведи приватный ключ на экран и скопируй его (от -----BEGIN до END-----):
bash Скопировать Открыть блок
Шаг 2: Добавляем секреты в GitHub
Зайди в свой репозиторий на GitHub -> Settings -> Secrets and variables -> Actions.
Нажми New repository secret. Добавь три секрета:
Имя: SERVER_IP, Значение: IP твоего сервера (например, 85.119.xx.xx)
Имя: SERVER_USER, Значение: имя пользователя сервера (обычно root или ubuntu)
Имя: SSH_PRIVATE_KEY, Значение: вставь тот огромный ключ, который скопировал в Шаге 1.
Шаг 3: Создаем пайплайн (Скрипт автодеплоя)
В папке с твоим проектом Nuxt на твоем компьютере создай папку .github, в ней папку workflows, а в ней файл deploy.yml.

Путь должен получиться такой: твой-проект/.github/workflows/deploy.yml

Вставь в этот файл следующий код:

yaml Скопировать Открыть блок
⚠️ Важный нюанс: Файл .env
Так как папка .output будет полностью заменяться при каждом деплое, твой .env файл не должен храниться внутри неё.

Где хранить переменные окружения? Лучший способ — прописать их прямо в настройки PM2.

Зайди на сервер и создай файл ecosystem.config.cjs в папке /var/www/nuxt-app:

javascript Скопировать Открыть блок
Теперь запусти проект не обычной командой, а через этот конфиг:

bash Скопировать Открыть блок
Как это выглядит теперь:
Ты написал новую фичу (допустим, добавил в корзину промокоды). Делаешь git commit -m "add promocodes" -> git push origin main. Идёшь пить кофе. Через 2 минуты новая версия автоматически крутится на сервере, а PM2 перезапустился без простоев сервиса. Красота! 😎

 Скопировать
 Gemini 3.1 Pro
22.04.2026 08:15:48 GMT+3
Ок хороший план, вывели все что мы решили по переносу нашего проекта nuxt nitro supabase

 Скопировать
Logo
Отличный, боевой план! Чтобы у вас перед глазами была четкая дорожная карта, я собрал всё, что мы обсудили, в единый пошаговый чеклист для переезда.

Это ваш генеральный план архитектуры: один российский сервер, на котором крутится база данных, бэкенд, фронтенд, и всё это обновляется само при пуше в Git.

Этап 1: Инфраструктура и Домены
Аренда сервера в РФ. Берем VPS/VDS (Яндекс.Облако, Selectel, Timeweb) с характеристиками минимум 4 ядра CPU, 8 ГБ RAM, 40+ ГБ SSD на базе Ubuntu 22.04/24.04.
Настройка доменов. В панели регистратора домена создаем две А-записи, указывающие на IP нового сервера:
твой-домен.ru — для Nuxt (пользовательский интерфейс).
api.твой-домен.ru — для Supabase (API и БД).
Этап 2: Развертывание Supabase (ЦУПАБЭС)
Установка Docker. Ставим на сервер Docker и Docker Compose.
Запуск из репозитория. Клонируем официальный репозиторий Supabase.
Настройка безопасности. В файле .env меняем пароли для Postgres, генерируем новые JWT-секреты, ANON_KEY и SERVICE_ROLE_KEY. Указываем SITE_URL = https://api.твой-домен.ru.
Запуск. Поднимаем контейнеры (docker-compose up -d). Supabase начнет работать на порту 8000.
Этап 3: Миграция данных из Облака на свой Сервер
База данных. Делаем дамп базы из облачного Supabase через pg_dump и разворачиваем его на своем сервере через pg_restore. Пользователи, пароли и таблицы перенесутся разом.
Файлы (Storage). Скачиваем картинки блюд из облака и переносим в бакеты на новом сервере (через админку Supabase Studio или скриптом).
Этап 4: Подготовка сервера под Nuxt + Nitro
Окружение. Устанавливаем Node.js (v20) и глобальный пакет PM2.
Папка проекта. Создаем директорию на сервере (например, /var/www/nuxt-app).
Управление переменными. Создаем файл ecosystem.config.cjs для PM2. В нем описываем запуск проекта на порту 3000 и указываем все секреты: SUPABASE_URL, SUPABASE_KEY и другие .env переменные. Это безопасно и удобно при автодеплое.
Этап 5: Настройка маршрутизации и SSL (Nginx)
Установка Nginx. Ставим веб-сервер.
Конфигурация Proxy. Пишем конфиг, который распределяет трафик:
Запросы на твой-домен.ru отправляются на localhost:3000 (Nuxt).
Запросы на api.твой-домен.ru отправляются на localhost:8000 (Supabase).
Шифрование (HTTPS). Устанавливаем Certbot и запускаем certbot --nginx. Он бесплатно выпустит SSL-сертификаты для обоих доменов (обязательно для работы Telegram Mini Apps).
Этап 6: Настройка Автодеплоя (GitHub Actions)
Мы не собираем проект на сервере, чтобы не тратить оперативную память базы данных!

Ключи. Генерируем SSH-ключ на сервере и добавляем его в GitHub Secrets вместе с IP и логином сервера.
Пайплайн. В репозитории создаем файл .github/workflows/deploy.yml.
Логика CI/CD. При пуше в ветку main GitHub сам:
Скачает зависимости (npm install).
Соберет проект (npm run build).
Удалит старую папку .output на сервере и скопирует новую по SSH.
Выполнит команду pm2 restart my-nuxt-app.
Итог: Что вы получаете после переезда
100% соответствие ФЗ-152: Все данные россиян лежат в дата-центре РФ.
Независимость: Никаких лимитов на количество запросов в базу, как в бесплатном или Pro тарифах облачного Supabase.
Производительность: Nuxt Nitro (серверный рендеринг и API) и база данных находятся на одной машине, пинг между ними составляет 0 миллисекунд.
DevEx (Удобство разработки): Код пушится в Git, через 2 минуты всё обновлено на сервере без вашего участия, и проект работает стабильно.
План монолитный и проверенный. Если будете двигаться по этим шагам — переезд займет 1-2 рабочих дня. Удачной миграции! Если на каком-то этапе возникнут затыки (особенно с Nginx или конфигами Докера) — пишите, разберем.