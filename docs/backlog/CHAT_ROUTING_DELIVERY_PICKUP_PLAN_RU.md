# TODO: Режимы маршрутизации чатов для доставки и самовывоза

> **Частично сделано (2026-05):** переназначение филиала и статусы в Telegram-чате — [`features/ORDER_CHAT_OPERATOR_FLOW_RU.md`](../features/ORDER_CHAT_OPERATOR_FLOW_RU.md).  
> План режимов автобалансировки и настроек в дашборде — [`features/ORDER_BRANCH_LOAD_BALANCING_RU.md`](../features/ORDER_BRANCH_LOAD_BALANCING_RU.md).

## Идея

- Для `delivery` использовать отдельный чат с менеджером (централизованный поток).
- Для `pickup` маршрутизировать заказы по чатам филиалов.
- Режимы должны переключаться в настройках дашборда.

## Зачем

- Менеджеры видят общую картину загрузки филиалов.
- Можно оперативно перераспределять заказы между филиалами.
- Уменьшается риск перегруза одного филиала при неравномерной нагрузке.

## Что сделать (MVP)

- [ ] Добавить настройку в дашборде: режим маршрутизации для `delivery` и `pickup`.
- [ ] Реализовать логику маршрутизации сообщений/заказов в нужный чат по выбранному режиму.
- [x] Добавить возможность менеджеру менять филиал-исполнитель для заказа (Telegram, кнопки в групповом чате).
- [ ] Показать в интерфейсе менеджера текущую нагрузку по филиалам (базовый индикатор).
- [ ] Зафиксировать правила fallback (если чат филиала недоступен -> в общий менеджерский чат).

## Вопросы перед реализацией

- [ ] Нужен ли отдельный режим "все в общий чат" независимо от типа выполнения?
- [ ] Кто имеет право переключать режимы: только owner или manager тоже?
- [ ] Нужен ли журнал изменений маршрутизации (audit trail)?

## Критерии приемки

- [ ] При `delivery` заказ попадает в чат менеджера согласно настройке.
- [ ] При `pickup` заказ попадает в чат выбранного/назначенного филиала согласно настройке.
- [x] Менеджер может переназначить филиал из Telegram-чата; новый филиал получает уведомление (см. feature-doc).
- [ ] Переключение режима в дашборде применяется без ручных правок кода.
